{
  "skill": "register",
  "description": "Register the current project with the dynos-work global registry at ~/.dynos/.",
  "input_schema": {},
  "output_schema": {
    "registration_result": {
      "type": "object",
      "description": "Registration result from dynoregistry.py"
    }
  }
}
